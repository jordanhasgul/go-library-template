name: ci

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

concurrency:
  cancel-in-progress: true
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}

permissions:
  contents: read

jobs:
  check-gomod:
    name: Check Go module is tidy
    runs-on: ubuntu-latest
    steps:
      - name: Checkout and setup Go repository
        uses: ./.github/actions/setup-go-repository

      - name: Check tidiness
        run: go mod tidy && git diff --exit-code

  lint:
    name: Run linters
    runs-on: ubuntu-latest
    steps:
      - name: Checkout and setup Go repository
        uses: ./.github/actions/setup-go-repository

      - name: Run golangci-lint
        uses: golangci/golangci-lint-action@v6
        with:
          version: v1.59

  test:
    name: Run tests
    strategy:
      fail-fast: false
      matrix:
        os: [ ubuntu-latest, macos-latest, windows-latest ]
        go-version: [ "stable", "oldstable" ]
    runs-on: ${{ matrix.os }}
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Checkout and setup Go repository
        uses: ./.github/actions/setup-go-repository

      - name: Run Go test
        run: go test -v -race -cover -coverprofile=coverage.out -covermode=atomic ./...

      - name: Generate coverage report
        run: go tool cover -html=coverage.out -o coverage.html

      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report-${{ matrix.os }}-${{ matrix.go-version }}
          path: coverage.html

  check-vulns:
    name: Check for vulnerabilities
    runs-on: ubuntu-latest
    steps:
      - name: Checkout and setup Go repository
        uses: ./.github/actions/setup-go-repository

      - name: Run govulncheck
        uses: golang/govulncheck-action@v1